# Deployment Configuration for deploy-to-coolify.ps1
# Copy this file to .env.deploy.local and fill in your values
# This file is git-ignored for security

# ========================================
# SSH/SERVER CONFIGURATION
# ========================================

# SSH username for deployment server
SERVER_USER=rndev

# SSH hostname or IP address
SERVER_HOST=rndev.local

# Password for sudo operations (if needed)
# Consider using SSH keys instead for better security
SUDO_PASSWORD=

# ========================================
# NOTES
# ========================================
# - Deployment uses simple git-based triggers (no API keys needed)
# - Files are synced to /home/{user}/chess-app-builds/
# - Coolify watches the GitHub repo for changes
# - Use trigger-deploy.ps1 to initiate deployments
# - All values can be overridden via command-line parameters