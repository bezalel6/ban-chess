# Deployment Configuration for deploy-to-coolify-integrated.ps1
# Copy this file to .env.deploy.local and fill in your values
# This file is git-ignored for security

# ========================================
# COOLIFY CONFIGURATION
# ========================================

# Your Coolify application ID (found in Coolify dashboard URL)
COOLIFY_APP_ID=ooswg4c4c8scs8kkcog4gcwl

# Coolify API token for automatic deployments (optional)
# Get from: Coolify > Settings > API Tokens
COOLIFY_API_TOKEN=

# Your Coolify instance URL (no trailing slash)
COOLIFY_URL=https://coolify.yourdomain.com

# ========================================
# SSH/SERVER CONFIGURATION
# ========================================

# SSH username for deployment server
SERVER_USER=root

# SSH hostname or IP address
SERVER_HOST=0.0.0.0

# Password for sudo operations (used with sshpass)
# Consider using SSH keys instead for better security
SUDO_PASSWORD=pass

# ========================================
# OPTIONAL OVERRIDES
# ========================================

# Custom Coolify base path (rarely needed)
# COOLIFY_BASE=/data/coolify/applications

# Force specific deployment path (if not using default)
# DEPLOYMENT_PATH=~/chess-app-builds

# ========================================
# NOTES
# ========================================
# - All values can be overridden via command-line parameters
# - Command-line parameters take precedence over .env values
# - Use quotes for values with spaces: SERVER_HOST="my server.local"
# - Comments start with # and are ignored
# - Empty values are treated as unset