# Production Environment Variables
# These are used when building and running in production

# Public variables (exposed to browser)
NEXT_PUBLIC_WEBSOCKET_URL=wss://chess-api.rndev.site
NEXT_PUBLIC_API_URL=https://chess.rndev.site
NEXT_PUBLIC_ENABLE_SOUNDS=true
NEXT_PUBLIC_ENABLE_ANALYSIS=false

# Server-side variables
NODE_ENV=production
NEXTAUTH_URL=https://chess.rndev.site
# NEXTAUTH_SECRET should be set in deployment environment, not in this file

# WebSocket server configuration
ALLOWED_ORIGINS=https://chess.rndev.site

# Database (set in deployment environment)
# DATABASE_URL=

# Redis for WebSocket state
REDIS_URL=redis://redis:6379