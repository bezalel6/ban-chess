# =====================================
# 2ban-2chess Coolify Environment Variables
# =====================================
# Copy this file and fill in the values in Coolify's environment variables section
# DO NOT commit this file with actual values to Git!

# =====================================
# REQUIRED VARIABLES (Must be set in Coolify)
# =====================================

# NextAuth Secret - Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-here-minimum-32-characters

# =====================================
# AUTO-PROVIDED BY COOLIFY (DO NOT SET)
# =====================================
# These are automatically set by Coolify, don't add them:
# - GITHUB_REPOSITORY (from your GitHub connection)
# - GITHUB_SHA (from deployment)
# - PORT (managed by Coolify)

# =====================================
# OPTIONAL VARIABLES (Only if needed)
# =====================================

# OAuth Providers (uncomment and fill if using social login)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Custom Redis Configuration (only if not using default)
# By default, uses redis://redis:6379 (no password)
# REDIS_URL=redis://redis:6379

# Custom allowed origins (if different from defaults)
# ALLOWED_ORIGINS=https://chess.rndev.site,https://ws-chess.rndev.site

# =====================================
# VARIABLES TO REMOVE (Not needed)
# =====================================
# If you see these in your Coolify env, remove them:
# - DATABASE_URL (no database yet)
# - REDIS_PASSWORD (removed password for simplicity)
# - NEXT_PUBLIC_API_URL (not used)
# - Any localhost URLs
# - NODE_ENV (set in docker-compose)
# - HOSTNAME (set in docker-compose)

# =====================================
# DEBUG FLAGS (Remove after testing)
# =====================================
# These are set in docker-compose.coolify.yml
# Only override here if you need different values:
# DEBUG=*
# LOG_LEVEL=debug
# VERBOSE=true